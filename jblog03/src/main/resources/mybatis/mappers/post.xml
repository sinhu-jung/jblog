<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">
	<insert id="insertWrite" parameterType="postvo">
		<![CDATA[
		insert 
			into post
		values(null, #{title }, #{contents }, now(), #{categoriNo }) 
		]]>
	</insert>
	
	<delete id="delete" parameterType="long" >
		<![CDATA[
		delete 
			from post 
		where categori_no=#{categoryNo } 
		]]>
	</delete>
	
	<select id="getpost" parameterType="map" resultType="postvo">
	<choose>
		<when test="postNo == 0">
			<![CDATA[
			select title, contents 
				from post 
			where no=(select min(no) from post where categori_no = #{categoryNo })
			]]>
		</when>
		<otherwise>
			<![CDATA[
			select title, contents from post where categori_no=#{categoryNo } and no=#{postNo }
			]]>
		</otherwise>
	</choose>
	</select>
	
	<select id="getPostList" parameterType="long" resultType="postvo">
		<![CDATA[
		select no, title, contents, reg_date as regDate, categori_no as categoriNo from post where categori_no=#{categoryNo }
		]]>
	</select>
</mapper>